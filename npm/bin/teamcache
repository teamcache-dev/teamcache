#!/usr/bin/env node
const { spawnSync } = require("child_process");
const result = spawnSync("teamcache", process.argv.slice(2), {
  stdio: "inherit",
  shell: process.platform === "win32",
});
process.exit(result.status ?? 1);
